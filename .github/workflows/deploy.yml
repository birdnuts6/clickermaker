      - name: Build Static Web Customizer
        uses: yawkat/web-openscad-editor@main
        with:
          config: '${{ github.workspace }}/editor.toml' # Force it to look at the exact file path
